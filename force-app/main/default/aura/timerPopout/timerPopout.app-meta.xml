<?xml version="1.0" encoding="UTF-8"?>
<AuraDefinitionBundle xmlns="http://soap.sforce.com/2006/04/metadata">
    <apiVersion>61.0</apiVersion>
    <description>Standalone chromeless Aura app hosting the pop-out time tracker window. Opened via window.open('/c/timerPopout.app', ...) from timerUtility so the timer stays visible in a separate browser window while the user works.</description>
</AuraDefinitionBundle>
