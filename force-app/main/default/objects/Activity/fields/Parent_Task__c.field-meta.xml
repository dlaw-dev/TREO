<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent_Task__c</fullName>
    <description>Id (as text) of the master task in a subtask chain. Not a real Lookup because the platform does not allow Task to be a lookup target on the Activity object (self-referencing Task relationships are unsupported). Blank on the master task itself and on ordinary manually-created tasks.</description>
    <externalId>false</externalId>
    <label>Parent Task</label>
    <length>18</length>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
