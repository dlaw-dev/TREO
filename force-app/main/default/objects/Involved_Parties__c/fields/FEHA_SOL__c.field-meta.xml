<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FEHA_SOL__c</fullName>
    <formula>IF(Exclude_FEHA_SOL__c, NULL, IF(ISBLANK(Term_Date__c), NULL, IF(Government__c, ADDMONTHS(Term_Date__c, 6) - 1, DATE(YEAR(Term_Date__c)+2, MONTH(Term_Date__c), DAY(Term_Date__c)) - 1)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>FEHA SOL</label>
    <required>false</required>
    <type>Date</type>
</CustomField>
